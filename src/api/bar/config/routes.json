{
  "routes": [
    {
      "method": "GET",
      "path": "/bars",
      "handler": "bar.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/bars/count",
      "handler": "bar.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/bars/:id",
      "handler": "bar.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/bars",
      "handler": "bar.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/bars/:id",
      "handler": "bar.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/bars/:id",
      "handler": "bar.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
